/** Shared auto-dismiss dwell time for the app's transient toast bubbles
 * (Toast.tsx and NoticeToast.tsx) — one constant so sibling toasts don't
 * disagree on how long they stay up. */
export const TOAST_DWELL_MS = 3500;
